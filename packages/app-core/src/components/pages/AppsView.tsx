/**
 * Apps catalog view — intentionally blank.
 *
 * The previous catalog UI (grid, running-apps row, search, curated sections)
 * has been removed ahead of a planned redesign around a unified plugin/app
 * manifest. This stub keeps the component export stable so `AppsPageView` and
 * other consumers continue to mount without changes.
 */
export function AppsView() {
  return <div className="apps-view apps-view--blank" />;
}
